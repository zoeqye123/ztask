---
name: ztask
description: 将模糊想法转化为结构化的可执行 Task 文件（Markdown 格式），遵循 zTask 任务规范，包含执行上下文、Phase 步骤和验收标准。可选运行 scan-paths.sh 生成 paths-context.md 以了解系统环境。当用户说"制定 Task"、"写个任务"、"我想做 X"、"把这个想法变成任务"，或需要生成结构化任务文件时使用。zTask 只负责制定，不负责执行。
---

# zTask — Task 制定 Skill

> **zTask 只负责制定 Task，不执行。** 当用户想把模糊想法变成结构化任务指令时激活。

## 激活条件

满足任一即激活：

- 用户说"帮我制定 Task"/"写个任务"/"安排一下"/"做个计划"
- 用户说"我想做 X"/"需要完成 Y"
- 用户描述了一个目标，想让它变成可执行的指令

## 执行流程

```
Task Progress:
- [ ] Step 1: 提问澄清
- [ ] Step 2: 读取系统上下文（如有需要）
- [ ] Step 3: 生成 Task
- [ ] Step 4: 输出与存放建议
```

### Step 1: 提问澄清

一次问清楚：

**必问：**

- **做什么？** 具体交付什么？（不是"做好一点"）
- **在哪做？** 涉及哪些目录/项目/文件？
- **做到什么程度？** 怎么算完成了？

**选问：**

- **有什么约束？** 不能踩的雷区？
- **有时间限制吗？** 截止日期？
- **有什么资源？** 已有素材/参考资料？

### Step 2: 读取系统上下文

- 若当前目录存在 `paths-context.md`，先读取它了解用户的文件结构，帮助推断执行文件夹。
- 若不存在且用户需要基于本机环境制定任务（如"帮我找个合适的目录执行"），运行扫描脚本生成：

```bash
bash scripts/scan-paths.sh paths-context.md
```

> 脚本位于 zTask 仓库 `scripts/scan-paths.sh`，扫描 Git 项目、AI 工具 Skills/MCP 与 Obsidian Vault，输出 `paths-context.md`。依赖：bash + python3。



### Step 3: 生成 Task

按以下模板生成，**必须包含**：背景、执行上下文、目标（checkbox）、Phase 步骤、验收标准。

```markdown
# Task: {领域}｜{描述}

## 背景
{为什么做这件事}

## 执行上下文
| 字段 | 值 |
|------|-----|
| **📁 执行文件夹** | `/路径/` |
| **🛠 关联 Skills** | `{如果有}` |

## 目标
- [ ] {具体目标1}
- [ ] {具体目标2}

## 执行步骤
### Phase 1 · {阶段名}
{步骤...}

## 验收标准
- [ ] {可验证的标准1}
- [ ] {可验证的标准2}
```



### Step 4: 输出

1. 输出完整 Task 文件内容（Markdown 格式）
2. 告知存放路径建议（如 `~/Obsidian Vault/01 Tasks/Task-xxx.md`）
3. 说明**下一步由用户决定**——用哪个 AI 执行、如何执行，zTask 不负责

完成后告知：

```
✅ Task 已生成，文件内容如上。

⚠️ zTask 只负责制定，不负责执行。
   下一步由你决定——用哪个 AI、用什么方式执行。
```



## 禁止事项

- ❌ 不问清楚就生成（"做好一点"不算 Task）
- ❌ 生成后擅自说明如何执行
- ❌ 超出制定范围，主动执行或管理任务
- ❌ 不告知存放路径



## 文件路径参考


| 文件        | 路径                             |
| --------- | ------------------------------ |
| 路径上下文     | `paths-context.md`（同目录下，由脚本生成） |
| 扫描脚本      | `scripts/scan-paths.sh`（仓库根目录） |
| Task 格式规范 | `TASK-FORMAT.md`               |


