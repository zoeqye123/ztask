# Task 格式规范

## 标准模板

```markdown
- [ ] Task-{领域}-{描述} 🔺 📅 YYYY-MM-DD 🏷️ #task 📁 /执行/路径/

# Task: {领域}｜{描述}

## 背景
为什么做这件事？解决了什么问题？

## 执行上下文
| 字段 | 值 |
|------|-----|
| **📁 执行文件夹** | `/执行/路径/` |
| **🛠 关联 Skills** | `skill-name` |

## 目标
- [ ] 目标 1

## 执行步骤
### Phase 1 · 阶段名

步骤...

## 验收标准
- [ ] 标准 1
```

## Signpost 行（Tasks 插件）

Task 文件**第一行**必须是 signpost 行：

```
- [ ] Task-任务名 🔺 📅 YYYY-MM-DD 🏷️ #task 📁 /执行/路径/
```

| 符号 | 含义 |
|------|------|
| `🔺` | 高优先级 |
| `🔻` | 中优先级 |
| `📅 YYYY-MM-DD` | 截止日期 |
| `📁 /路径/` | AI 执行时的**工作目录**（cwd） |
| `🏷️ #task` | 标签 |
| `- [x] ✅ 日期` | 已完成 |

**最小配置：**
```
- [ ] Task-我的任务 🔺 🏷️ #task
```

## 执行上下文（关键）

告诉 AI **在哪执行**、**用什么工具**：

- `📁 执行文件夹`：AI 的 `cwd`，文件读写都在这里
- `🛠 关联 Skills`：需要调用的 Skill 路径

## Task Loop

```
制定 Task（Obsidian）
    ↓
scan-paths.sh（生成路径上下文）
    ↓
AI 读取 Task + 路径上下文
    ↓
执行 → 交付物
    ↓
Archive（标记 done，移入 history/）
    ↓
沉淀为知识 → 下一个 Task 的上下文
```

**每次的交付物，就是下一次 Task 的上下文。路径上下文让 AI 知道你的文件在哪。**
